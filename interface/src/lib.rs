#![warn(missing_docs)]
//! interface represents abstract data type which defines whole operations and its semantics for a data structure.

/// Provide list interface
pub mod list;
/// Provide queue related interface
pub mod queue;
/// Provide set releated interface
pub mod set;
